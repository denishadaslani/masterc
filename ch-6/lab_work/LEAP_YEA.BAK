#include<stdio.h>
#include<conio.h>
main()
{
	int first,second;
	clrscr();
	printf("enter the firstyear and secondyear number:");
	scanf("%d %d",&first,&second);

	while(first<=second)
	{
	       if((first %4 == 0 && first % 100 !=0) ||(first % 400 ==0))
	       {
		     printf("%d\t",first);
	       }

	       first++;
	}

	getch();

}