#include<stdio.h>
#include<conio.h>
main()
{
	int num;
	int sum=0;
	int rem;
	int orignalnum;

	clrscr();
	printf("enter any number:");
	scanf("%d",&num);
	orignalnum=num;
	while(orignalnum!=0)
	{
	     rem=orignalnum%10;
	     sum+=rem*rem*rem;
	     orignalnum/=10;
	}

	if(sum==num)

	{
	  printf("%d is armstrong",num);

	}
	else
	{

	  printf("%d is not armstrong",num);
	}
	getch();
}
